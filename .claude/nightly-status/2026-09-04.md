# Nightly Run — 2026-09-04

## Daily Health Check

**Branch hygiene:** `git fetch --all` run. Only branches present: `master` (local, up to date with `origin/master`) and `origin/main` (known unrelated GitHub Pages branch — left alone per standing instruction). Nothing ahead of master; no merges or deletions needed.

**Functional smoke test:** SKIPPED — infrastructure constraint, not an app failure.
- `preview_list` showed no running dev server, and `preview_start` for the `brigid-pro` config was refused with: "Dev servers can't be started from unattended sessions (scheduled-task runs and remote-dispatched trees) — nobody is present to approve the command."
- Test credentials (`BRIGID_TEST_PT_EMAIL`/`PASSWORD`, `BRIGID_TEST_CLIENT_EMAIL`/`PASSWORD`) were present in `.env.local`, so this was not a credentials gap.
- This is the same standing constraint documented in the 2026-08-12 and 2026-09-03 runs; it has not been lifted. No bug report filed — nothing about the app was observed to fail, the check simply could not run.
- Considered the live-site API-script workaround the weekly task used on 2026-09-03 (`scratchpad/auth.mjs` hitting `www.tripleaspect.fit` directly), but did not use it here: (a) that weekly run's own P0 finding is that production has been serving a stale Preview-only build since 2026-08-11, so testing against it would not exercise current `master`; (b) this daily task's smoke test is specified as a browser-driven UI check (console errors on `/pt/clients` and `/portal/<clientId>`), which an API-only script can't verify anyway.

**Programme save regression check:** SKIPPED — same infrastructure constraint as above (requires the same live dev server and browser session). Not exercised this run.

**Typecheck:** `npx tsc --noEmit` — clean, no errors. Nothing to fix. (Incidental `tsconfig.tsbuildinfo` diff from running tsc was reverted rather than committed, since it wasn't accompanied by any real change.)

**Commit/push:** No source code changes this run. Committed this status file only. Pushed to `origin/master`.

**Standing flag (carried forward, unchanged):** the smoke test and regression check steps remain unreachable in a fully unattended context as currently specified. Worth revisiting whether this scheduled task should get a dev-server-start exception, or whether these checks need a non-browser-driven path (e.g. a script hitting the API directly against a properly-current environment) to run reliably unattended. Separately — unresolved from 2026-09-03 and outside this task's authority to fix — production deploys are still reportedly Preview-only (Vercel Production Branch likely set to `main` instead of `master`); worth a human checking whether that's been corrected since.
