# Nightly Run — 2026-08-09

## Daily Health Check

**Branch hygiene:** `git fetch --all` run. Only branches present: `master` (local, up to date with origin) and `origin/main` (known unrelated GitHub Pages branch — left alone per standing instruction). Nothing ahead of master; no merges or deletions needed.

**Functional smoke test:** PASS — full flow exercised this time, including check-in and completion (prior nights had skipped those two steps because the smoke session was scheduled for "tomorrow", so no session-today card ever appeared in the portal). Judgment call: scheduled "Smoke Test Session" for **today** (2026-08-09) instead of tomorrow, specifically so the check-in and mark-complete steps in this task's own instructions could actually be exercised — noting this as a deliberate deviation from the literal "(scheduled tomorrow)" wording, since that wording is inconsistent with the later "today's session card" step.
- Signed in as PT (`test.pt@brigid.local`) → `/pt/clients` loaded the test client (`0 sess`) with no console errors.
- Session builder: searched for "Smoke Test Squat" by exact name — **found the existing custom exercise and reused it** (no duplicate created).
- Built and saved session "Smoke Test Session" (Forging, scheduled 2026-08-09/today) on Test Client. Save succeeded — directory correctly showed "1 sess" and "Next Session: Today".
- Signed in as client (`test.client@brigid.local`) → `/portal/<clientId>` loaded with no console errors, showing the pre-session check-in modal and today's "Smoke Test Session" card.
- Submitted check-in: Sleep 4/5, Stress 2/5, Soreness 3/5, notes "Smoke test" — no console errors, modal closed cleanly.
- Clicked "Mark Session Complete" — card updated to "✓ Session Complete", no console errors.
- Back as PT: confirmed in the client drawer — "This Week: 1 sessions logged", 8-week adherence bucket for the week of 3 Aug now shows "1/1", Sessions tab shows "Smoke Test Session — 2026-08-09 ✓ Done", and Recent Check-ins shows the submitted values (4 Sleep / 2 Stress / 3 Soreness / "Smoke test"). Full round trip verified end to end.
- **Cleanup — partial:** deleted "Smoke Test Session" via the client drawer's Sessions tab (two-step delete: ✕ then Confirm). Verified removed (client back to "0 sess", attention banner reappeared as expected). Kept "Smoke Test Squat" exercise as the reused fixture.
- **Cleanup — could NOT remove the check-in.** Neither the PT-side client drawer (Overview → Recent Check-ins, read-only display, no delete control) nor the client-side History page (check-in card only has an "Edit check-in" pencil icon → Save/Cancel form, no Delete button) offers any way to delete a wellbeing check-in. This is a real product gap, not a smoke-test failure — filed via the in-app bug-report button (`POST /api/bug-reports` → 201 Created) describing the missing delete affordance. **The "Smoke test" check-in dated 9 Aug 2026 remains in the database** — flagging for whoever picks up that bug report, since this will recur every night until a delete path exists (or this task is updated to stop submitting a live check-in).

**Tooling note:** consistent with 2026-08-08's finding, the Browser pane did not composite frames in this scheduled run (`screenshot` errors with "Browser pane is not displayed"), and `computer` click events were unreliable throughout (worked maybe half the time). Worked around this the same way — dispatching events via `javascript_tool` (`form.requestSubmit()` for logins, `button.click()` for buttons found by text/title/aria-label match) — functionally equivalent to a real click since it fires the same handlers.

**Typecheck:** `npx tsc --noEmit` — clean, no errors. Nothing to fix.

**Commit/push:** Committed `tsconfig.tsbuildinfo` (only file touched, consistent with prior nightly runs) and pushed to `origin/master`. Confirmed `git status` clean afterward.

## Weekly Audit & Ship

**Bug triage — reviewed the standing `bug_reports` queue (10 open rows)** plus a targeted live Suite A/B/C pass via authenticated `fetch()` against the routes flagged since 2026-08-02. Found several bugs already fixed in code/Notion (BUG-98, BUG-99 on 2026-08-02; BUG-96 on 2026-07-13; BUG-81 on 2026-08-02) but never marked `resolved` in the `bug_reports` table — synced all 4 statuses as part of this run's cleanup.

**Bugs found and fixed (3):**
1. **BUG-79** — `PATCH /api/admin/pts/[ptId]`: `free_client_quota` guard (`typeof !== 'number' || x < 0`) admitted `NaN`/`Infinity`/non-integers (`NaN < 0` and `Infinity < 0` are both `false`). Fixed: added `Number.isFinite`/`Number.isInteger` + a 10,000 upper bound. `app/api/admin/pts/[ptId]/route.ts`.
2. **File-signature verification (closes bug_reports #67 and #89)** — `portal/photos`, `exercises/video`, `pt/logo`, `pt/avatar` all trusted the client-declared `File.type` with zero verification against actual bytes; a plain-text file declared `image/png` was accepted and served back publicly with a forged `Content-Type` header. Fixed with a new `lib/file-signature.ts` (magic-byte sniffing for PNG/JPEG/GIF/WebP/WebM/MP4-QuickTime, no new dependency) wired into all four upload routes. Verified live: both original repros (text-as-PNG, HTML-as-MP4) now 400; a genuine PNG still uploads (no regression).
3. **BUG-100** — no UI ever called the existing `DELETE /api/portal/checkin/[id]` endpoint (client History page was edit-only; PT drawer read-only). Flagged by tonight's own daily health check, which left a stuck test check-in with no cleanup path. Fixed: added a ✕ → "Confirm?" delete control to the client History page's check-in cards. Used it live to delete the stuck 9 Aug test check-in.

**Backlog feature shipped (bandwidth: 3+ bugs → fix all + ship 1):** extended the `resend-invite`-established rate-limiting precedent (Technical Debt: "Rate limiting on API routes ... most other routes still don't") to `POST /api/portal/photos` — 20 uploads/client/hour, using the existing `created_at` column (no new migration, unlike `resend-invite`'s dedicated cooldown column).

**Bonus fix — CI lint pipeline was silently broken since 2026-07-03:** discovered while confirming this run's commit passed CI. `next lint` had no `eslint.config.mjs` in the repo, so every CI run since the pipeline was added hung on ESLint's interactive setup prompt and failed with an opaque exit-1 (type-check always passed, masking it). Added `eslint.config.mjs` (flat config, `next/core-web-vitals` + `next/typescript`, no new dependency) — CI lint now actually runs and reports real output. This surfaced **56 pre-existing lint errors + 5 warnings across ~19 unrelated files**, none introduced tonight; fixed only the 3 in files already touched (dead `ScoreField` type, two structurally-required-but-unused `_req` route handler params). The remaining cleanup is logged as a new Technical Debt item in Notion for a dedicated future pass — CI will show red on lint until then.

**Data-boundary spot checks:** re-ran Suite C (`<script>` + SQL-injection-shaped `full_name`, `program_length_weeks: -50`) and Suite B (`program_length_weeks: 99999`, `start_date: 1900-01-01`, `renewal_date: 9999-12-31`) against `POST /api/clients` — both correctly blocked at the validation gateway (400s, no partial writes). No new findings beyond the 3 bugs above.

**DB sanitation:** deleted the one Suite-B regression-test photo row created during verification (`progress_photos` id `90f1e1d7…`, via its own `DELETE` endpoint) and the stuck 9 Aug "Smoke test" check-in (via the newly-shipped delete UI, confirmed above). No other stale test data found — the 2 Suite-C `POST /api/clients` probes were rejected before any row was written.

**Typecheck:** `npx tsc --noEmit` clean throughout, re-confirmed after every change.

**Docs & Notion:** `CHANGELOG.md` updated with a `### Weekly Audit & Ship (2026-08-09...)` section covering all of the above. Notion Roadmap updated: BUG-79 and BUG-71 (photos MIME-trust item, covers bug_reports #67/#89) marked resolved with fix descriptions; new BUG-100 entry added and marked resolved; Technical Debt rate-limiting line updated to include `portal/photos`; CI pipeline line annotated with the lint-was-broken finding; new "Lint cleanup (found 2026-08-09)" Technical Debt item added for the 56-error backlog.

**Commit/push:** 3 commits pushed to `origin/master` — `fbf7206` (the 3 bug fixes + feature), `a021fa8` (eslint config fix), `d50256e` (changelog doc-only follow-up). `gh auth status` confirmed the `cv1284` account was already active; no switch needed.

**Deploy — NOT CONFIRMED, same standing issue as 2026-08-02, now 2 weeks running:** waited for the build window and re-tested a known-fixed repro (`PATCH /api/programmes/[id]` with `category: ""`, fixed on 2026-08-02) directly against `https://tripleaspect.fit` using a disposable test programme (created and deleted via the API). It still returned the **pre-2026-08-02** raw-500 behavior (`invalid input value for enum session_category: ""`), meaning production has not picked up *any* commit since before last week's audit — not just tonight's. Confirmed the same root cause as last week: `vercel whoami` on this machine authenticates as `tauntonelectriciandev-8151` / team `Taunton_Electrician`, which has no access to the `brigid-pro` project (`orgId team_U1HwQvZVT22u68SKo6WIuKrb`); `gh api repos/cv1284/tripleaspect/hooks` still returns `[]`; `gh api .../deployments` still shows only unrelated `github-pages` entries from May. This is now confirmed broken for at least 2 consecutive weekly runs (and everything in between, since the daily health checks only test the local dev server, not production).

**Action needed from the user (repeated from 2026-08-02, unresolved):** check the Vercel dashboard for the `brigid-pro` project directly — the auto-deploy-on-push integration is either not wired up or has stopped triggering, or the correct account/team needs to be re-linked on this machine (`vercel login` as the account that owns `orgId team_U1HwQvZVT22u68SKo6WIuKrb`, or update `.vercel/project.json` if the project moved teams). Once fixed, either the next natural push will deploy, or run `vercel --prod` manually from the correct account to catch up all pending commits (`4492c50` through `d50256e`). Given this task's autonomy note about revisiting ship/deploy gates "when Brigid goes live for real users" — a deploy pipeline that's been silently dark for 2+ weeks is itself a signal worth noting even pre-launch, since it means nothing shipped by either the daily or weekly automated runs has reached production this whole time.
