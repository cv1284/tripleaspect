# Nightly Run — 2026-08-12

## Daily Health Check

**Branch hygiene:** `git fetch --all --prune` run. Only branches present: `master` (local, up to date with `origin/master`) and `origin/main` (known unrelated GitHub Pages branch — left alone per standing instruction). Nothing ahead of master; no merges or deletions needed.

**Functional smoke test:** SKIPPED — infrastructure constraint, not an app failure.
- `preview_list` showed no running dev server, and port 3000 was not listening.
- This run is unattended (scheduled-task session), and the environment explicitly blocks starting a dev server from an unattended session ("nobody is present to approve the command"). No prior running server was available to reuse, so the browser-driven PT/client flow (session builder, check-in, completion, drawer confirmation, cleanup) could not be exercised this run.
- Test credentials (`BRIGID_TEST_PT_EMAIL`/`PASSWORD`, `BRIGID_TEST_CLIENT_EMAIL`/`PASSWORD`) were present in `.env.local`, so this was not a credentials gap.
- No bug report filed — nothing about the app was observed to fail; the check simply could not run.

**Programme save regression check:** SKIPPED — same infrastructure constraint as above (requires the same live dev server and browser session). Not exercised this run. Note: last night's (2026-08-11) run left an orphaned empty programme titled `ORPHANED TEST ARTIFACT — please delete (2026-08-11 nightly, empty, no items)` under `test.pt@brigid.local` still awaiting manual deletion (blocked by the destructive-action permission classifier in an unattended run) — carrying this note forward since it wasn't addressed tonight either.

**Typecheck:** `npx tsc --noEmit` — clean, no errors. Nothing to fix.

**Commit/push:** No source code changes this run. Committed this status file only. Pushed to `origin/master`.

**Flag for follow-up:** if nightly runs are expected to regularly execute in a fully unattended context (no human present to approve a dev-server start), the smoke test and regression check steps in this task's instructions are currently unreachable — worth revisiting whether the dev-server-start restriction should have an exception for this scheduled task, or whether these checks need a non-browser-driven path (e.g. a script hitting the API directly) to be reliably run unattended.
