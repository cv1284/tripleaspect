# Nightly Run — 2026-09-03

## Daily Health Check

**Branch hygiene:** `git fetch --all` run. Only branches present: `master` (local, up to date with `origin/master`) and `origin/main` (known unrelated GitHub Pages branch — left alone per standing instruction). Nothing ahead of master; no merges or deletions needed.

**Functional smoke test:** SKIPPED — infrastructure constraint, not an app failure.
- `preview_list` showed no running dev server, and `preview_start` for the `brigid-pro` config was refused with: "Dev servers can't be started from unattended sessions (scheduled-task runs and remote-dispatched trees) — nobody is present to approve the command."
- No prior running server was available to reuse, so the browser-driven PT/client flow (session builder, check-in, completion, drawer confirmation, cleanup) could not be exercised this run.
- Test credentials (`BRIGID_TEST_PT_EMAIL`/`PASSWORD`, `BRIGID_TEST_CLIENT_EMAIL`/`PASSWORD`) were present in `.env.local`, so this was not a credentials gap.
- No bug report filed — nothing about the app was observed to fail; the check simply could not run. This is the same constraint documented in the 2026-08-12 run; it has not been lifted since.

**Programme save regression check:** SKIPPED — same infrastructure constraint as above (requires the same live dev server and browser session). Not exercised this run.

**Typecheck:** `npx tsc --noEmit` — clean, no errors. Nothing to fix.

**Commit/push:** No source code changes this run. Committed this status file only. Pushed to `origin/master`.

**Note on run cadence:** The previous status file on disk is dated 2026-08-12 — there's a ~3-week gap with no nightly status files before tonight. Not something this run can diagnose (scheduling/dispatch is outside this task's scope), but worth someone checking whether the scheduled task ran during that window and simply didn't produce output, or didn't fire at all.

**Standing flag (carried forward):** if nightly runs are expected to regularly execute in a fully unattended context (no human present to approve a dev-server start), the smoke test and regression check steps in this task's instructions remain unreachable as currently specified. Worth revisiting whether the dev-server-start restriction should have an exception for this scheduled task, or whether these checks need a non-browser-driven path (e.g. a script hitting the API directly) to run reliably unattended.
</content>
